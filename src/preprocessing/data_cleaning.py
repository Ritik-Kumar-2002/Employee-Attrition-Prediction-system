

def clean_data(df):

    # Remove duplicate value (No duplicate present)
    # df.duplicated().sum() # check duplicate

    # Remove Null value (No Null value)
    # df.isnull().sum() # check Null values

    return df